{{#each erros }}
    <div class="alert alert-danger">
        {{texto}}
    </div>
{{else}}

{{/each}}

<div class="card">
    <div class="card-body">

<form  action="/admin/postagens/nova" method="Post">

  <label for="nome">titulo</label>
  <input type="text" id="titulo" name="titulo" placeholder="titulo da postagem" class="form-control">
  <br>
  <label for="slug">slug</label>
  <input type="text" id="slug" name="slug" placeholder="slug" class="form-control">
  <br>
  <label for="slug">Descricao</label>
  <input type="text" id="descricao" name="descricao" placeholder="descricao" class="form-control">
  <br>   
  <label for="slug">Conteudo</label> 
  <textarea name="conteudo" class="form-control">{{conteudo}}</textarea>
    <br>
    <label for="categoria">Categoria</label> 
  <select name="categoria" class="custom-select">    
      {{#each categorias}}
      <option value="{{this.categoria._id}}">{{this.nome}}</option>
      {{/each}}
  </select>
  <br>
  <button type="submit" class="btn btn-success mt-4">Salvar</button>
</form>

</div>

</div>